# SusokuSolverML
An AI agent that uses Python, KNN algorithm and OpenCV to autonomously solve web-based Sudoku puzzles. It finds the grid on screen, reads the digits, solves the logic, and types the solution automatically.
